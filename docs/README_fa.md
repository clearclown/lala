# ویرایشگر لالا

**زبان‌های دیگر**: [English](../README.md) | [日本語](README_ja.md) | [العربية](README_ar.md) | [简体中文](README_zh-CN.md) | [繁體中文](README_zh-TW.md) | [Русский](README_ru.md)

ویرایشگر متن مدرن و سبک با پشتیبانی از پیش‌نمایش چندقالبی، ساخته شده با Rust.

## ویژگی‌ها

### رابط گرافیکی (GUI)

- 📁 **نمای درختی فایل‌ها** با بارگذاری ناهمگام
- 🎨 **برجسته‌سازی نحو** برای زبان‌های برنامه‌نویسی مختلف
- 🔍 **جستجو و جایگزینی پیشرفته** با پشتیبانی از عبارات منظم
- ⌨️ **میانبرهای صفحه‌کلید** برای کارایی بیشتر
- 🖥️ **رابط کاربری مدرن** با استفاده از egui/eframe

### رابط خط فرمان (CLI)

- 📝 **پیش‌نمایش Markdown** با قالب‌بندی رنگی
- 🌐 **پیش‌نمایش HTML** با رندر متن ساده
- 📊 **پیش‌نمایش نمودار Mermaid** به صورت هنر ASCII
- 📐 **پیش‌نمایش LaTeX** با نمادهای یونیکد ریاضی
- 🎨 **خروجی رنگی** (قابل غیرفعال‌سازی)
- 🚀 **سرعت بالا** بدون وابستگی به WebView

## نصب

### Cargo (crates.io)

```bash
cargo install lala
```

### Linux

#### Debian/Ubuntu

```bash
sudo apt install lala
```

یا از فایل `.deb`:

```bash
wget https://github.com/yourusername/lala/releases/latest/download/lala-linux-x86_64.deb
sudo dpkg -i lala-linux-x86_64.deb
```

#### Arch Linux

```bash
yay -S lala
```

یا:

```bash
paru -S lala
```

#### Fedora/RHEL

```bash
sudo dnf install lala
```

یا:

```bash
sudo yum install lala
```

#### نصب جهانی (Linux/macOS)

```bash
curl -sSL https://raw.githubusercontent.com/yourusername/lala/main/packaging/scripts/install.sh | bash
```

### macOS

#### Homebrew

```bash
brew install lala
```

### Windows

#### Chocolatey

```bash
choco install lala
```

#### Scoop

```bash
scoop install lala
```

#### نصب‌کننده

فایل نصب‌کننده را از [صفحه انتشارات](https://github.com/yourusername/lala/releases) دانلود کنید.

## استفاده

### حالت GUI

```bash
# باز کردن ویرایشگر
lala

# باز کردن فایل خاص
lala file.txt

# باز کردن پوشه
lala /path/to/directory
```

### حالت CLI - پیش‌نمایش Markdown

```bash
# پیش‌نمایش فایل Markdown
lala preview document.md

# پیش‌نمایش بدون رنگ
lala preview --no-color document.md
```

### حالت CLI - پیش‌نمایش HTML

```bash
# پیش‌نمایش فایل HTML
lala html index.html

# پیش‌نمایش بدون رنگ
lala html --no-color page.html
```

### حالت CLI - پیش‌نمایش Mermaid

```bash
# پیش‌نمایش نمودار Mermaid
lala mermaid diagram.mmd

# پیش‌نمایش بدون رنگ
lala mermaid --no-color flowchart.mmd
```

انواع نمودارهای پشتیبانی‌شده:
- نمودارهای جریان (Flowchart)
- نمودارهای توالی (Sequence Diagram)
- نمودارهای کلاس (Class Diagram)
- نمودارهای حالت (State Diagram)
- نمودارهای ER (Entity Relationship)
- نمودارهای گانت (Gantt Chart)
- نمودارهای دایره‌ای (Pie Chart)

### حالت CLI - پیش‌نمایش LaTeX

```bash
# پیش‌نمایش سند LaTeX
lala latex document.tex

# پیش‌نمایش بدون رنگ
lala latex --no-color paper.tex
```

ویژگی‌های پشتیبانی‌شده:
- حروف یونانی (α, β, γ, ...)
- عملگرهای ریاضی (√, ∫, ∑, ∏, ...)
- کسرها و معادلات
- زیرنویس و بالانویس
- ماتریس‌ها

### راهنمای کامل

برای اطلاعات بیشتر، مستندات ما را مطالعه کنید:

- [راهنمای نصب](fa/نصب.md)
- [راهنمای استفاده از CLI](fa/استفاده-CLI.md)
- [پشتیبانی از قالب‌ها](fa/پشتیبانی-قالب.md)
- [راهنمای مشارکت](../CONTRIBUTING.md)

## ساخت از منبع

### پیش‌نیازها

- Rust 1.70 یا بالاتر
- Cargo
- کتابخانه‌های سیستمی:
  - Linux: `libxcb-dev`
  - macOS: بدون نیاز به کتابخانه اضافی
  - Windows: بدون نیاز به کتابخانه اضافی

### مراحل ساخت

```bash
# کلون کردن مخزن
git clone https://github.com/yourusername/lala.git
cd lala

# ساخت نسخه انتشار
cargo build --release

# اجرای تست‌ها
cargo test

# نصب محلی
cargo install --path .
```

## معماری

ویرایشگر لالا از معماری مدولار استفاده می‌کند:

```
lala/
├── src/
│   ├── main.rs          # نقطه ورود برنامه
│   ├── app.rs           # منطق برنامه اصلی GUI
│   ├── cli/             # ماژول‌های رابط خط فرمان
│   │   ├── mod.rs       # تنظیم CLI و پارسینگ آرگومان
│   │   ├── markdown_view.rs   # رندر Markdown
│   │   ├── html_view.rs       # رندر HTML
│   │   ├── mermaid_view.rs    # رندر نمودار Mermaid
│   │   └── latex_view.rs      # رندر LaTeX
│   ├── editor/          # کامپوننت‌های ویرایشگر
│   └── file_tree/       # نمای درختی فایل‌ها
├── tests/               # تست‌های یکپارچگی
└── packaging/           # اسکریپت‌های بسته‌بندی
```

## مشارکت

ما از مشارکت‌ها استقبال می‌کنیم! لطفا [راهنمای مشارکت](../CONTRIBUTING.md) ما را بخوانید.

### حوزه‌های مشارکت

- 🐛 گزارش و رفع باگ‌ها
- ✨ پیشنهاد و پیاده‌سازی ویژگی‌های جدید
- 📝 بهبود مستندات
- 🌍 ترجمه به زبان‌های دیگر
- 🎨 بهبود رابط کاربری
- ⚡ بهینه‌سازی عملکرد

## لایسنس

این پروژه تحت لایسنس دوگانه منتشر شده است:

- لایسنس MIT ([LICENSE-MIT](../LICENSE-MIT))
- لایسنس Apache 2.0 ([LICENSE-APACHE](../LICENSE-APACHE))

می‌توانید هر کدام را که ترجیح می‌دهید انتخاب کنید.

## تشکر

ویرایشگر لالا از کتابخانه‌های متن‌باز عالی زیر استفاده می‌کند:

- [egui](https://github.com/emilk/egui) - چارچوب رابط کاربری
- [ropey](https://github.com/cessen/ropey) - ساختار داده طناب برای متن
- [pulldown-cmark](https://github.com/raphlinus/pulldown-cmark) - پارسر Markdown
- [syntect](https://github.com/trishume/syntect) - برجسته‌سازی نحو
- [html2text](https://github.com/jugglerchris/rust-html2text) - تبدیل HTML به متن
- و بسیاری دیگر...

## پیوندها

- 🏠 [صفحه اصلی](https://github.com/yourusername/lala)
- 📦 [Crates.io](https://crates.io/crates/lala)
- 📖 [مستندات](https://github.com/yourusername/lala/tree/main/docs)
- 🐛 [گزارش مشکلات](https://github.com/yourusername/lala/issues)
- 💬 [بحث‌ها](https://github.com/yourusername/lala/discussions)

---

ساخته شده با ❤️ توسط جامعه لالا
